{
    "name": "mentormatch",
    "version": "1.0.0",
    "description": "AI-Powered Mentorship Matching Platform",
    "scripts": {
        "render-build": "cd server && npm install && cd ../client && npm install && npm run build",
        "start": "node server/src/index.js"
    },
    "engines": {
        "node": ">=18"
    }
}